<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hotplego">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotpleGO">
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivityLogin"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".MainActivitySign"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".MainActivitySearch"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".SelectRegister"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".MainActivitySignMg"
            android:parentActivityName=".MainActivity" />



    </application>

</manifest>